# ==============================================================================
# React Vite Frontend Configuration
# ==============================================================================

# API Base URL (defaults to Vite proxy /api/v1 during development)
VITE_API_BASE_URL=/api/v1

# Target Backend Server (used by Vite dev proxy)
VITE_BACKEND_URL=http://localhost:8000

