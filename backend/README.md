# CKAN Unified FastAPI Backend

Unified Python backend combining REST API, PostgreSQL, Neo4j, and PyTorch CKAN recommendation engine.

Managed with `uv`:
```bash
uv venv
uv sync
uv run uvicorn app.main:app --reload --port 8000
```

